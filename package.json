{
  "name": "project-stack",
  "private": true,
  "scripts": {                                                                                            
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "deploy:api": "bash deploy.sh"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "prettier": "^3.7.4",
    "turbo": "^2.9.5",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@9.9.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
